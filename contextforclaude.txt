OVOA/Jarvis: Expo iOS app (jarvis/app) + Cloudflare Worker (jarvis/api, D1 jarvis-db). Ships via Codemagic -> TestFlight. UI is the white "one day, one spine" design with a left drawer; not yet checked on a phone.
2026-09-22 speed + many-users pass (commits 747338c server, 6a7e0b3 app), NOT DEPLOYED: wrangler is logged into the wrong Cloudflare account (edgeformmarketing, not edgeformmedia), so run `npx wrangler login` as edgeformmedia, then from jarvis/api `npm run db:migrate` (0032 is required: drainNotes reads agent_notes.hold_until) and `npm run deploy`, then push main (that starts the TestFlight build).
Speed: stable prompt prefix (clock/steps moved onto the last user message) + Workers AI x-session-affinity; server voices spoken replies inside the /chat stream when the app sends `speak` (voice.ts speechStream, app voice.ts serverSpeech); first clause emitted early; 60 s session cache; brief cached 15 min; memory pass only on first-person messages (remember.ts).
Many users: ratelimits bindings RL_AUTH/TURN/SPEAK/LOGS (limits.ts; if deploy rejects them, delete the block in wrangler.jsonc — code allows when a binding is missing); cron in two leased lanes (cron_lock) + per-person sweeps sliced 1-in-5 (sweep.ts); app resets per-person state on sign-out via lib/signOut.ts registry, handles dead sessions and offline launch.
Verified locally only (tsc, 16 unit tests, smoke 185/185, web preview). First on-device check: device_logs "first voiced piece after N ms" vs "first sentence after".
