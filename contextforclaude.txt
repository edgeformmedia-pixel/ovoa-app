OVOA/Jarvis: Expo SDK 57 iOS app (jarvis/app) + Cloudflare Worker with D1 (jarvis/api). Ships via Codemagic -> TestFlight on push to main; deploy the Worker with XDG_CONFIG_HOME=C:/Users/thoma/.wrangler-edgeformmedia (migrate -> deploy -> curl -> push main).
2026-09-23 menu redo: the drawer is only Talk, Apps, Account, Settings (free plan: Today instead of Talk). The app opens on Talk ((tabs)/index.tsx redirects; Activity moved to /activity). Every other screen is an add-on in lib/addons.ts (name, "by OVOA", one-line about, search keywords, preinstalled/dev flags), installed per phone from (tabs)/apps.tsx with search; press and hold to remove. Each add-on carries a usage level (none/some/more of the daily allowance in api/src/plans.ts): Background Work and Live Listen are "more".
Account ((tabs)/account.tsx) took plan, name/email, password, sign out, delete account out of Settings. A one-time 6-card tour (components/Tour.tsx, lib/tour.ts key ovoa.tour.v1) shows after the setup conversation and on first open of this version; Settings > Getting around replays it. The tour is spoken (OVOA voice with a plan, phone voice on free), opens the real menu, rings each row (lib/drawer.ts pointAt) and taps it; min reading time per card so a failed voice can't race it.
Create (Apps top card -> app/create.tsx): say or type -> POST /apps/design (model) -> preview -> POST /apps (D1 user_apps, migration 0037, api/src/myapps.ts). A made app opens in Talk (lib/activeApp.ts); /chat takes `app` and its instructions ride on that message only. Verified live 2026-09-23 with a throwaway account. DeepSeek was out of balance (402) then, so chat fell back to glm (10-37 s replies).
Checked on Expo web at 390 wide against the local Worker (tour, menu, Apps install/search/open, Account, Settings). Not yet seen on a phone.
Plans (docs/paywall/SPEC.md): gating built but OFF until MEMBERSHIP_API_KEY is on the Worker; everyone is pro until then.
