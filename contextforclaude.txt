OVOA/Jarvis: Expo SDK 57 iOS app (jarvis/app) + Cloudflare Worker with D1 (jarvis/api) at https://api.ovoa.ai on the ovoa.ai Cloudflare account e58b0ec5305410f9d3cd70f461f39cb6 (pinned in wrangler.jsonc). Ships via Codemagic -> TestFlight on push to main; deploy the Worker from jarvis/api with XDG_CONFIG_HOME=C:/Users/thoma/.wrangler-ovoa (migrate -> deploy -> curl -> push main). The old edgeformmedia account now only holds the forwarder at jarvis-api.edgeformmedia.workers.dev (jarvis/forwarder) and the old D1 as a backup: delete both after 2026-10-14.
2026-09-23 made apps have their own screen: app/made/[id].tsx (hero in the app's colour, ask bar with mic, parts from components/AppBlocks.tsx: buttons, checklist, counter, log, timer, note) and app/made/edit.tsx (components/AppEditor.tsx: live preview, "change it by saying so" -> POST /apps/revise, look, parts, behaviour). Server api/src/myapps.ts + migration 0038 (blocks, state, speak); taps -> POST /apps/:id/state (applyOp, tested); while open the model gets the screen and an app_update tool (verified on prod: "out of milk and eggs, drank two glasses" filled the list and counter).
Setup/tour fixes (audited by a second agent): setup useConversation({wake:false, fillers:false, answers:true}) — turnGate `answers` stops one-word answers being dropped as echo; mic permission asked before the greeting; Skip closes the mic while the next question is read; goodbye said before finish. speak() has no filler by default. Talk won't listen until the tour is seen (assistant.tsx shouldListen), band/click ignored while held; drawer open/close resolve when the spring lands and the tour measures after that; tour card moves up for low targets; drawer swipes locked during the tour.
Server setup: gym keeps "7 days a week 1-2pm", parts of the day map to times, a named med with no time is asked about, acks can't promise, JSON retry without ack. Checked on web (local Worker 8788 + jarvis-web-clear 8083) and prod curl; not yet seen on a phone.
