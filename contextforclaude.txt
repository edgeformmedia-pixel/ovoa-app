OVOA/Jarvis: Expo iOS app (jarvis/app) + Cloudflare Worker (jarvis/api, D1 jarvis-db). Ships via Codemagic -> TestFlight.
UI is rebuilt on the approved white "one day, one spine" design: lib/theme.ts is now the whole vocabulary (colours, space, type, radius) and nothing else; teal = now/your turn only, violet = the agent's own hand. The bottom tab bar is gone — navigation is a hand-rolled left drawer (components/Drawer.tsx, core Animated + PanResponder, zero new deps, works in Expo Go). Journal is now Day and carries the timeline spine (components/Spine.tsx); Activity is health only; components/Feed.tsx and NotesBadge.tsx are deleted, their content is on the spine and the drawer.
Every dev surface is still reachable: dev-tools, live listen and Ask Claude are in the drawer's Developer group, Settings/Transcripts/Report a problem in More, the Logs toggle is still top-right of Talk.
Not verified on a phone: this machine is Windows, so the whole rework was checked with `npx tsc --noEmit` and `npx expo start --web` at 320 and 375 pt. The drawer's edge-drag gesture in particular has never run on iOS. Needs a real device pass.
Known gaps, all reported in docs/ui-rework-prompt.md's terms: no "where you were" on workouts and no smooth bpm line (no route data, no react-native-svg); the Brief's "what it has learned" is an empty state with a TODO(brief-learning); the splash screen is still on the old near-black background.
