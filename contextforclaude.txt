Added Apple Health to the Activity tab (heart rate + 12 h graph, resting, sleep, energy,
exercise, HRV, stand, today's workouts) via components/HealthCards.tsx + lib/health.ts.
Added per-turn timing (lib/turnTimer.ts, Dev tools -> Turn timings, perf log kind), server
per-tool and per-prompt-section logging, a buzz + spoken filler + spoken failures on band
turns, and a more conversational voice prompt. API deployed. Measurements and the next
optimizations are in docs/voice-latency.md. Still BLOCKED on: `eas init` in jarvis/app.
