OVOA/Jarvis: Expo SDK 57 iOS app (jarvis/app) + Cloudflare Worker with D1 (jarvis/api). Ships via Codemagic -> TestFlight; deploy the Worker with XDG_CONFIG_HOME=C:/Users/thoma/.wrangler-edgeformmedia (migrate -> deploy -> curl -> push main).
2026-09-22 cost pass (docs/cost-pass.md, brief in docs/cost-cut-prompt.md) is committed, deployed and pushed: usage_daily counts every person's tokens/voice/mic/clips/searches; engines, voice engine and clip transcriber switch at run time (server_settings, Dev tools, PUT /debug/engines); the phone hears its name on-device (modules/name-ear) and streams to Deepgram only after it; typed turns carry a 31-tool core; release builds upload warn+ and timing lines only (Send logs in Dev tools); TURN_CAP_MONTHLY=1000; web search and OSRM answers are cached.
Not yet seen on a phone: the native ear, the phone's own voice, the log row count. Check docs/cost-pass-testflight.md against device_logs when the TestFlight build lands.
Engines: gpt-oss-120b on Workers AI answers spoken turns; GLM (Z.ai, GLM_API_KEY set) is cheapest but slow to the first word; DeepSeek has no credit; Gemini key is free tier. Benchmarks in docs/voice-latency.md.
Website (ovoa-team): paid early access via Stripe; the app is not gated on membership (setup.md Part C).
