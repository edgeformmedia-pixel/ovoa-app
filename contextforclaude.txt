OVOA/Jarvis: Expo iOS app (jarvis/app) + Cloudflare Worker (jarvis/api, D1 jarvis-db). Ships via Codemagic -> TestFlight.
Last: money feature added (src/money.ts, migration 0028) — balance/pay/bills/plans, "can I afford this" with the trade-off named.
BLOCKED: `npm run db:migrate` fails — the wrangler OAuth token has no d1 scope. Run `npx wrangler login`, then migrate + deploy.
Not deployed yet; worker still on the pre-money build. Tests and the 157-check smoke pass locally.
Next: re-auth, migrate + deploy, then build #58 on Codemagic for the pending app-side voice fixes.
